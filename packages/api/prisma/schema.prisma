// =============================================================================
// COFORMA STUDIO - PRISMA SCHEMA
// =============================================================================
// Database: PostgreSQL 15+ with Row-Level Security (RLS)
// ORM: Prisma 5+
// Multi-Tenancy: Single database with tenant_id column on all tables

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions", "multiSchema"]
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  // uuid_ossp MUST carry map: the Postgres extension is literally named
  // "uuid-ossp". Without the map Prisma emits CREATE EXTENSION "uuid_ossp",
  // which no Postgres has, so any generated migration would fail on apply.
  extensions = [pgcrypto, uuid_ossp(map: "uuid-ossp")]
}

// =============================================================================
// ENUMS
// =============================================================================

enum TenantRole {
  ADMIN        // Full access to tenant settings and all features
  FACILITATOR  // Can manage CABs, sessions, and moderate content
  MEMBER       // CAB member with read/contribute access
}

enum InviteStatus {
  PENDING
  ACCEPTED
  DECLINED
  EXPIRED
}

enum SessionStatus {
  SCHEDULED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum FeedbackType {
  IDEA
  BUG
  REQUEST
  RESEARCH_INSIGHT
}

enum FeedbackStatus {
  OPEN
  UNDER_REVIEW
  PLANNED
  IN_PROGRESS
  SHIPPED
  CLOSED
}

enum FeedbackPriority {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum ActionItemStatus {
  TODO
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum IntegrationProvider {
  ZOOM
  GOOGLE_MEET
  SLACK
  JIRA
  ASANA
  CLICKUP
  GOOGLE_CALENDAR
  HUBSPOT
  SALESFORCE
  PHYNDCRM        // MADFAM ecosystem CRM (engagement source-of-truth)
}

enum IntegrationStatus {
  CONNECTED
  DISCONNECTED
  ERROR
}

enum DiscountType {
  PERCENTAGE  // e.g., 20% off
  FIXED       // e.g., $50 off
}

enum BadgeType {
  FOUNDING_PARTNER
  INFLUENCER
  TOP_CONTRIBUTOR
  EARLY_ADOPTER
  POWER_USER
  STRATEGIC_ADVISOR
}

enum CaseStudyStatus {
  DRAFT
  PENDING_APPROVAL
  APPROVED
  PUBLISHED
}

/// PersonaRole - Who the member represents in their org (a16z framework).
/// Sessions reviewing strategic decisions should require BUYER attendance;
/// product feedback sessions should require END_USER. Used by the validator
/// in session.service.ts.
enum PersonaRole {
  BUYER         // Holds budget / signs the contract.
  END_USER      // Uses the product day-to-day.
  CHAMPION      // Internal advocate making the case for adoption.
  EXECUTIVE     // C-suite sponsor with strategic context.
}

/// MembershipExitStatus - Lifecycle outcome of a design-partner engagement.
/// Driven by a16z framing: success = GRADUATED_TO_PAID. ACTIVE while the
/// cohort window is open; CHURNED when the partner disengages without
/// converting; RENEWED for partners who extend a second cohort window.
enum MembershipExitStatus {
  ACTIVE
  GRADUATED_TO_PAID
  CHURNED
  RENEWED
}

/// EngagementCadence - How often the design partner is expected to engage.
/// a16z's default recommendation is BIWEEKLY for active design-partner
/// cohorts. WEEKLY for very-early-stage discovery; MONTHLY for late-stage
/// renewal-track partners.
enum EngagementCadence {
  WEEKLY
  BIWEEKLY
  MONTHLY
  QUARTERLY
}

// =============================================================================
// CORE MODELS
// =============================================================================

/// Tenant (Organization) - Top-level entity for multi-tenancy
model Tenant {
  id        String   @id @default(uuid()) @db.Uuid
  slug      String   @unique // Subdomain slug (e.g., "acme" -> acme.coforma.studio)
  name      String
  domain    String?  @unique // Custom domain (e.g., cab.acme.com)
  logo      String? // Cloudflare R2 URL
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // PhyndCRM federation: a Coforma tenant maps to a PhyndCRM tenant
  // (via PhyndCRM's tenantId string). Nullable so MADFAM-internal
  // tenants without a CRM presence don't break.
  phyndcrmTenantId String? @unique @map("phyndcrm_tenant_id") // PhyndCRM tenantId (e.g. "madfam")

  // Branding & Customization
  brandColor      String? @map("brand_color") // Hex color (e.g., "#3B82F6")
  locale          String  @default("en") // en, es
  timezone        String  @default("America/Mexico_City")
  whitelabelEnabled Boolean @default(false) @map("whitelabel_enabled") // Phase 4 feature

  // Subscription & Billing
  stripeCustomerId       String?  @unique @map("stripe_customer_id")
  stripeSubscriptionId   String?  @unique @map("stripe_subscription_id")
  subscriptionStatus     String? @map("subscription_status") // active, trialing, past_due, canceled
  subscriptionPlanId     String? @map("subscription_plan_id")
  billingEmail           String? @map("billing_email")
  currentPeriodStart     DateTime? @map("current_period_start")
  currentPeriodEnd       DateTime? @map("current_period_end")
  trialEnd               DateTime? @map("trial_end")

  // Relationships
  memberships   TenantMembership[]
  cabs          CAB[]
  sessions      Session[]
  feedbackItems FeedbackItem[]
  actionItems   ActionItem[]
  integrations  Integration[]
  invites       Invite[]
  discountPlans DiscountPlan[]
  badges        Badge[]
  caseStudies   CaseStudy[]
  auditLogs     AuditLog[]

  @@map("tenants")
}

/// User - Individual user account (can belong to multiple tenants)
model User {
  id        String   @id @default(uuid()) @db.Uuid
  email     String   @unique
  name      String?
  avatar    String? // Cloudflare R2 URL
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // NextAuth.js fields
  emailVerified DateTime? @map("email_verified")
  image         String?

  // Relationships
  accounts         Account[]
  sessions         UserSession[]
  tenantMemberships TenantMembership[]
  cabMemberships   CABMembership[]
  feedbackItems    FeedbackItem[]
  actionItems      ActionItem[]
  comments         Comment[]
  votes            Vote[]
  sessionAttendees SessionAttendee[]
  referrals        Referral[]       @relation("Referrer")
  referredBy       Referral[]       @relation("Referred")
  badges           UserBadge[]
  auditLogs        AuditLog[]

  @@map("users")
}

/// Account - OAuth/OIDC provider accounts (NextAuth.js)
model Account {
  id                String  @id @default(uuid()) @db.Uuid
  userId            String  @db.Uuid @map("user_id")
  type              String
  provider          String
  providerAccountId String @map("provider_account_id")
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@map("accounts")
}

/// UserSession - NextAuth.js sessions (database strategy)
model UserSession {
  id           String   @id @default(uuid()) @db.Uuid
  sessionToken String   @unique @map("session_token")
  userId       String   @db.Uuid @map("user_id")
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("user_sessions")
}

/// VerificationToken - Email verification tokens (NextAuth.js)
model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
  @@map("verification_tokens")
}

/// TenantMembership - User membership in a tenant with role
model TenantMembership {
  id        String     @id @default(uuid()) @db.Uuid
  tenantId  String     @db.Uuid @map("tenant_id")
  userId    String     @db.Uuid @map("user_id")
  role      TenantRole
  createdAt DateTime   @default(now()) @map("created_at")
  updatedAt DateTime   @updatedAt @map("updated_at")

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([tenantId, userId])
  @@map("tenant_memberships")
}

// =============================================================================
// CAB (Customer Advisory Board) MODELS
// =============================================================================

/// CAB - Customer Advisory Board
model CAB {
  id          String   @id @default(uuid()) @db.Uuid
  tenantId    String   @db.Uuid @map("tenant_id")
  name        String
  description String?  @db.Text
  slug        String // Unique within tenant
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  // Settings
  isActive     Boolean @default(true) @map("is_active")
  maxMembers   Int? @map("max_members") // Optional member cap
  minMembers   Int? @map("min_members") // Floor: warn if active membership drops below (a16z 5-10 portfolio)
  requiresNDA  Boolean @default(false) @map("requires_nda")

  // Engagement window (a16z): every cohort runs for a defined period
  // (1/3/6 months typical) with a clean exit toward paid or churn at the end.
  // Nullable so legacy/open-ended CABs don't break.
  cohortLabel  String? @map("cohort_label") // e.g. "Tezca Spring 2026", "Q3 Pilot Wave"
  startDate    DateTime? @map("start_date")
  endDate      DateTime? @map("end_date")

  // Relationships
  tenant         Tenant          @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  members        CABMembership[]
  sessions       Session[]
  feedbackItems  FeedbackItem[]

  @@unique([tenantId, slug])
  @@index([tenantId])
  @@map("cabs")
}

/// CABMembership - User membership in a CAB with metadata
model CABMembership {
  id        String   @id @default(uuid()) @db.Uuid
  cabId     String   @db.Uuid @map("cab_id")
  userId    String   @db.Uuid @map("user_id")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // Member metadata
  tags         String[] // e.g., ["power_user", "influencer", "strategic"]
  company      String?
  title        String?
  ndaSigned    Boolean  @default(false) @map("nda_signed")
  ndaSignedAt  DateTime? @map("nda_signed_at")

  // a16z design-partner intake (3-criterion framework). All nullable to
  // keep legacy memberships working; new memberships should be scored
  // (representativeness/urgency/capacity 0-10) before promoting from
  // candidate to active. See migration 20260426000000.
  personaRole             PersonaRole? @map("persona_role")
  representativenessScore Int? @map("representativeness_score")
  urgencyScore            Int? @map("urgency_score")
  capacityScore           Int? @map("capacity_score")
  intakeJustification     String?              @db.Text @map("intake_justification")

  // a16z lifecycle. ACTIVE while the cohort window is open; transitions
  // to GRADUATED_TO_PAID / CHURNED / RENEWED at exit. exitedAt + exitNote
  // are set together with exitStatus by the exit mutation route. Schema
  // matches migration 20260426000000.
  exitStatus  MembershipExitStatus @default(ACTIVE) @map("exit_status")
  exitedAt    DateTime? @map("exited_at")
  exitNote    String?              @db.Text @map("exit_note")

  // PhyndCRM federation. Filled when:
  //  - Inbound webhook receives a PhyndCRM contact and the operator
  //    promotes it into this CAB, or
  //  - Outbound emitter creates a PhyndCRM contact when a member is
  //    added directly through Coforma.
  // Nullable so legacy memberships don't break. Indexed for dedup
  // lookups during inbound webhook processing.
  phyndcrmContactId    String? @map("phyndcrm_contact_id")
  phyndcrmEngagementId String? @map("phyndcrm_engagement_id") // Optional — links to a PhyndCRM engagement

  // Discount plan assignment
  discountPlanId String?   @db.Uuid @map("discount_plan_id")
  discountPlan   DiscountPlan? @relation(fields: [discountPlanId], references: [id], onDelete: SetNull)

  // a16z light-contract: every active design partner should have an
  // explicit engagement agreement (cadence + hours/month + expiry).
  partnershipAgreement PartnershipAgreement?

  // Relationships
  cab  CAB  @relation(fields: [cabId], references: [id], onDelete: Cascade)
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([cabId, userId])
  @@index([cabId])
  @@index([userId])
  @@index([phyndcrmContactId])
  @@index([cabId, exitStatus])
  @@map("cab_memberships")
}

/// PartnershipAgreement - a16z's "light contract" for a design partner.
///
/// The article is explicit: every design partner should have a written
/// agreement covering scope, cadence, expected time commitment, and
/// expiry. The contract isn't legalese — it's an alignment artifact.
/// `requiresNDA` on the parent CAB is a coarse Yes/No; this model
/// captures the per-membership specifics that the audit calls out as
/// missing.
///
/// 1:1 with CABMembership. Optional — historical CABs created before
/// this model shipped don't get back-filled.
model PartnershipAgreement {
  id              String            @id @default(uuid()) @db.Uuid
  membershipId    String            @unique @db.Uuid @map("membership_id")
  createdAt       DateTime          @default(now()) @map("created_at")
  updatedAt       DateTime          @updatedAt @map("updated_at")

  // Scope: 1-3 sentence description of what this partner is being asked
  // to participate in. Free-form because the discipline is in writing
  // it down, not in the schema.
  scope           String            @db.Text

  // Cadence + time budget. `expectedHoursPerMonth` is the planning
  // anchor — operators flag drift in the engagement-health computed
  // field when actual attendance falls below it.
  cadence         EngagementCadence @default(BIWEEKLY)
  expectedHoursPerMonth Int @map("expected_hours_per_month")

  // Window. Mirrors CAB.{startDate,endDate} semantics but per-membership
  // (a partner can join mid-cohort).
  effectiveStart  DateTime @map("effective_start")
  effectiveEnd    DateTime? @map("effective_end")

  // Acceptance trail — equivalent to ndaSigned/ndaSignedAt but for the
  // partnership agreement itself. NULL means not yet accepted.
  acceptedAt      DateTime? @map("accepted_at")
  acceptedByEmail String? @map("accepted_by_email") // Free-form — partner-side signer

  // Operator's notes on why this scope/cadence was set. Survives operator
  // turnover: the next person can read why we asked for monthly instead
  // of biweekly without asking the founder.
  notes           String?           @db.Text

  membership      CABMembership     @relation(fields: [membershipId], references: [id], onDelete: Cascade)

  @@index([effectiveEnd])
  @@map("partnership_agreements")
}

// =============================================================================
// SESSION MODELS
// =============================================================================

/// Session - CAB meeting/session
model Session {
  id          String        @id @default(uuid()) @db.Uuid
  tenantId    String        @db.Uuid @map("tenant_id")
  cabId       String        @db.Uuid @map("cab_id")
  title       String
  description String?       @db.Text
  status      SessionStatus @default(SCHEDULED)
  createdAt   DateTime      @default(now()) @map("created_at")
  updatedAt   DateTime      @updatedAt @map("updated_at")

  // Scheduling
  scheduledAt DateTime @map("scheduled_at")
  duration    Int       @default(60) // Duration in minutes
  endedAt     DateTime? @map("ended_at")

  // Meeting details
  meetingLink String? @map("meeting_link") // Zoom, Google Meet, etc.
  recordingUrl String? @map("recording_url") // Cloudflare R2 URL
  agendaItems Json? @map("agenda_items") // Array of {title, duration, completed}

  // a16z required-attendance gate. JSON array of PersonaRole values that
  // MUST be represented in attendees for this session. Strategic-review
  // sessions should require ['BUYER']; product-feedback should require
  // ['END_USER']. Validator in session.service.ts blocks scheduling if
  // unmet. Empty array = no requirement (legacy sessions).
  requiredRoles Json? @map("required_roles") // PersonaRole[] — see schema notes

  // Relationships
  tenant           Tenant            @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  cab              CAB               @relation(fields: [cabId], references: [id], onDelete: Cascade)
  attendees        SessionAttendee[]
  feedbackItems    FeedbackItem[]
  actionItems      ActionItem[]
  minutes          SessionMinute?

  @@index([tenantId])
  @@index([cabId])
  @@index([scheduledAt])
  @@map("sessions")
}

/// SessionAttendee - User attendance at a session
model SessionAttendee {
  id        String   @id @default(uuid()) @db.Uuid
  sessionId String   @db.Uuid @map("session_id")
  userId    String   @db.Uuid @map("user_id")
  createdAt DateTime @default(now()) @map("created_at")

  // Attendance tracking
  attended Boolean  @default(false)
  talkTime Int? @map("talk_time") // Talk time in seconds (from Zoom/Meet analytics)
  joinedAt DateTime? @map("joined_at")
  leftAt   DateTime? @map("left_at")

  session Session @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([sessionId, userId])
  @@index([sessionId])
  @@map("session_attendees")
}

/// SessionMinute - Meeting minutes and decisions
model SessionMinute {
  id        String   @id @default(uuid()) @db.Uuid
  sessionId String   @unique @db.Uuid @map("session_id")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // Content
  summary   String   @db.Text
  decisions String[] @db.Text // Array of decision strings
  notes     String?  @db.Text

  session Session @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@map("session_minutes")
}

// =============================================================================
// FEEDBACK MODELS
// =============================================================================

/// FeedbackItem - Customer feedback (idea, bug, request, research insight)
model FeedbackItem {
  id          String           @id @default(uuid()) @db.Uuid
  tenantId    String           @db.Uuid @map("tenant_id")
  cabId       String?          @db.Uuid @map("cab_id") // Optional: linked to specific CAB
  sessionId   String?          @db.Uuid @map("session_id") // Optional: linked to specific session
  userId      String           @db.Uuid @map("user_id") // Submitter
  title       String
  description String           @db.Text
  type        FeedbackType
  status      FeedbackStatus   @default(OPEN)
  priority    FeedbackPriority @default(MEDIUM)
  createdAt   DateTime         @default(now()) @map("created_at")
  updatedAt   DateTime         @updatedAt @map("updated_at")

  // Metadata
  tags        String[] // e.g., ["mobile", "performance", "ux"]
  attachments String[] // Cloudflare R2 URLs

  // External integration (Jira, Asana, ClickUp)
  externalId       String? @map("external_id") // External ticket ID
  externalProvider IntegrationProvider? @map("external_provider")
  externalUrl      String? @map("external_url")

  // Relationships
  tenant   Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  cab      CAB?      @relation(fields: [cabId], references: [id], onDelete: SetNull)
  session  Session?  @relation(fields: [sessionId], references: [id], onDelete: SetNull)
  user     User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  comments Comment[]
  votes    Vote[]

  @@index([tenantId])
  @@index([cabId])
  @@index([userId])
  @@index([status])
  @@index([type])
  @@map("feedback_items")
}

/// Comment - Comments on feedback items
model Comment {
  id             String   @id @default(uuid()) @db.Uuid
  feedbackItemId String   @db.Uuid @map("feedback_item_id")
  userId         String   @db.Uuid @map("user_id")
  content        String   @db.Text
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")

  feedbackItem FeedbackItem @relation(fields: [feedbackItemId], references: [id], onDelete: Cascade)
  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([feedbackItemId])
  @@map("comments")
}

/// Vote - Upvotes/downvotes on feedback items
model Vote {
  id             String   @id @default(uuid()) @db.Uuid
  feedbackItemId String   @db.Uuid @map("feedback_item_id")
  userId         String   @db.Uuid @map("user_id")
  value          Int      // +1 (upvote) or -1 (downvote)
  createdAt      DateTime @default(now()) @map("created_at")

  feedbackItem FeedbackItem @relation(fields: [feedbackItemId], references: [id], onDelete: Cascade)
  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([feedbackItemId, userId])
  @@index([feedbackItemId])
  @@map("votes")
}

// =============================================================================
// ACTION ITEM MODELS
// =============================================================================

/// ActionItem - Task/action item from sessions
model ActionItem {
  id          String           @id @default(uuid()) @db.Uuid
  tenantId    String           @db.Uuid @map("tenant_id")
  sessionId   String?          @db.Uuid @map("session_id")
  title       String
  description String?          @db.Text
  status      ActionItemStatus @default(TODO)
  dueDate     DateTime? @map("due_date")
  completedAt DateTime? @map("completed_at")
  createdAt   DateTime         @default(now()) @map("created_at")
  updatedAt   DateTime         @updatedAt @map("updated_at")

  // Assignment
  assignedToId String? @db.Uuid @map("assigned_to_id")
  assignedTo   User?   @relation(fields: [assignedToId], references: [id], onDelete: SetNull)

  tenant  Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  session Session? @relation(fields: [sessionId], references: [id], onDelete: SetNull)

  @@index([tenantId])
  @@index([sessionId])
  @@index([assignedToId])
  @@index([status])
  @@map("action_items")
}

// =============================================================================
// INCENTIVES & RECOGNITION MODELS
// =============================================================================

/// DiscountPlan - Discount plan for CAB members
model DiscountPlan {
  id        String       @id @default(uuid()) @db.Uuid
  tenantId  String       @db.Uuid @map("tenant_id")
  name      String
  type      DiscountType
  value     Decimal      @db.Decimal(10, 2) // e.g., 20.00 for 20%
  cap       Decimal?     @db.Decimal(10, 2) // Optional max discount amount
  expiresAt DateTime? @map("expires_at")
  createdAt DateTime     @default(now()) @map("created_at")
  updatedAt DateTime     @updatedAt @map("updated_at")

  // Renewal rules
  autoRenew         Boolean @default(false) @map("auto_renew")
  renewalDuration   Int? @map("renewal_duration") // Duration in months
  graduationPath    String? @db.Text @map("graduation_path") // Description of graduation to paid plan

  tenant  Tenant          @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  members CABMembership[]

  @@index([tenantId])
  @@map("discount_plans")
}

/// Badge - Achievement badge
model Badge {
  id          String    @id @default(uuid()) @db.Uuid
  tenantId    String    @db.Uuid @map("tenant_id")
  name        String
  description String?   @db.Text
  type        BadgeType
  icon        String? // Cloudflare R2 URL
  createdAt   DateTime  @default(now()) @map("created_at")

  tenant Tenant      @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  users  UserBadge[]

  @@index([tenantId])
  @@map("badges")
}

/// UserBadge - Badge awarded to a user
model UserBadge {
  id        String   @id @default(uuid()) @db.Uuid
  userId    String   @db.Uuid @map("user_id")
  badgeId   String   @db.Uuid @map("badge_id")
  awardedAt DateTime @default(now()) @map("awarded_at")
  reason    String?  @db.Text

  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)
  badge Badge @relation(fields: [badgeId], references: [id], onDelete: Cascade)

  @@unique([userId, badgeId])
  @@index([userId])
  @@map("user_badges")
}

/// Referral - Referral tracking for CAB members
model Referral {
  id          String   @id @default(uuid()) @db.Uuid
  referrerId  String   @db.Uuid @map("referrer_id") // User who referred
  referredId  String   @db.Uuid @map("referred_id") // User who was referred
  status      String   @default("pending") // pending, approved, rewarded
  rewardValue Decimal? @db.Decimal(10, 2) @map("reward_value") // Reward amount
  createdAt   DateTime @default(now()) @map("created_at")
  approvedAt  DateTime? @map("approved_at")

  referrer User @relation("Referrer", fields: [referrerId], references: [id], onDelete: Cascade)
  referred User @relation("Referred", fields: [referredId], references: [id], onDelete: Cascade)

  @@unique([referrerId, referredId])
  @@index([referrerId])
  @@map("referrals")
}

/// CaseStudy - Published case studies
model CaseStudy {
  id          String            @id @default(uuid()) @db.Uuid
  tenantId    String            @db.Uuid @map("tenant_id")
  title       String
  content     String            @db.Text
  status      CaseStudyStatus   @default(DRAFT)
  publishedAt DateTime? @map("published_at")
  createdAt   DateTime          @default(now()) @map("created_at")
  updatedAt   DateTime          @updatedAt @map("updated_at")

  // Metadata
  author      String? // Author name
  thumbnail   String? // Cloudflare R2 URL
  slug        String  // Unique within tenant

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, slug])
  @@index([tenantId])
  @@index([status])
  @@map("case_studies")
}

// =============================================================================
// INTEGRATION MODELS
// =============================================================================

/// Integration - Third-party service integration
model Integration {
  id        String              @id @default(uuid()) @db.Uuid
  tenantId  String              @db.Uuid @map("tenant_id")
  provider  IntegrationProvider
  status    IntegrationStatus   @default(CONNECTED)
  createdAt DateTime            @default(now()) @map("created_at")
  updatedAt DateTime            @updatedAt @map("updated_at")

  // OAuth credentials (encrypted)
  accessToken  String? @db.Text @map("access_token")
  refreshToken String? @db.Text @map("refresh_token")
  expiresAt    DateTime? @map("expires_at")

  // Provider-specific metadata
  metadata Json? // e.g., workspace ID, channel ID, etc.

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, provider])
  @@index([tenantId])
  @@map("integrations")
}

/// Invite - Email invitation to join CAB or tenant
model Invite {
  id        String       @id @default(uuid()) @db.Uuid
  tenantId  String       @db.Uuid @map("tenant_id")
  email     String
  status    InviteStatus @default(PENDING)
  token     String       @unique // Magic link token
  expiresAt DateTime @map("expires_at")
  createdAt DateTime     @default(now()) @map("created_at")
  acceptedAt DateTime? @map("accepted_at")

  // Invitation details
  cabId String? @db.Uuid @map("cab_id") // Optional: specific CAB invitation
  role  TenantRole @default(MEMBER)

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@index([email])
  @@map("invites")
}

// =============================================================================
// AUDIT & COMPLIANCE MODELS
// =============================================================================

/// AuditLog - Audit trail for compliance (GDPR, LGPD)
model AuditLog {
  id        String   @id @default(uuid()) @db.Uuid
  tenantId  String   @db.Uuid @map("tenant_id")
  userId    String?  @db.Uuid @map("user_id")
  action    String   // e.g., "user.created", "feedback.updated", "session.deleted"
  resource  String   // e.g., "User", "FeedbackItem", "Session"
  resourceId String? @db.Uuid @map("resource_id")
  metadata  Json?    // Additional context
  ipAddress String? @map("ip_address")
  userAgent String?  @db.Text @map("user_agent")
  createdAt DateTime @default(now()) @map("created_at")

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  user   User?  @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([tenantId])
  @@index([userId])
  @@index([action])
  @@index([createdAt])
  @@map("audit_logs")
}

// =============================================================================
// RLS POLICIES (Applied via migrations, not Prisma)
// =============================================================================
// NOTE: Row-Level Security policies are defined in SQL migrations.
// All queries must set `current_setting('app.tenant_id')` to enforce isolation.
//
// Example policy (applied in migration):
// CREATE POLICY tenant_isolation ON tenants
//   USING (id::text = current_setting('app.tenant_id'));
//
// See: packages/api/prisma/migrations/YYYYMMDDHHMMSS_enable_rls/migration.sql
